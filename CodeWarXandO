function XO(str) {
  let xCount = 0;
  let oCount = 0;

  for (let i = 0; i < str.length; i++) {
    const char = str[i].toLowerCase();
    if (char === 'x') {
      xCount++;
    } else if (char === 'o') {
      oCount++;
    }
  }

  return xCount === oCount;
}
